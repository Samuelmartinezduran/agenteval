# agenteval (core)

Librería y CLI para evaluar agentes LLM con function calling / tool use.

Ver el [README del proyecto](../../README.md) para la documentación completa.

```bash
agenteval run examples/suites/weather-agent.yaml
agenteval validate examples/suites/weather-agent.yaml
```
